# Prodigy_GA_01
**Short Description:**  This project uses the GPT-2 model to generate meaningful text by fine-tuning it on a custom dataset. The trained model produces coherent and context-aware text based on user-provided prompts using Python and NLP techniques.
